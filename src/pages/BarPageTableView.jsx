import BarPageNav from "../Components/BarComponents/BarpageNav";

const BarPageTableView = () => {
  return (
    <div className=" bg-main-dark-bg text-gray-100">
      <BarPageNav />
    </div>
  );
};

export default BarPageTableView;
